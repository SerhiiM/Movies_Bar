import * as api from '../api';
import axios from 'axios';
import * as cnst from '../common/constants';

export function loadInitDataForAdmin(dispatch,callback) {
    axios.all(
            [

            ]
        )
        .then(axios.spread(
                (

                ) => 
                    {
                        
                    }
                )
            )
        .then(() => {
            callback();
        })
}